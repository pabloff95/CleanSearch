## Build app

To build the app for dev testing follow the next steps:

1. Excute `yarn build-app`, this will generate the js code required to run the extension.
2. Copy the new generated files in `extension/dist`.
3. Open `chrome://extensions` in your browser.
4. Click on "Load unpacked" and select the dist file

The extension is now ready in your browser.
